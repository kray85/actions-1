name: Github Actions Variable and Secrets Example

on: 
  # push
  # schedule:
    # - cron: '*/1 * * * *'  # every 15 minutes
  workflow_dispatch:
env:
    CONTAINER_REGISTRY: docker.io
    IMAGE_NAME: github-actions-nginx

# jobs:

#     # Define env at step level
#     docker:
#         runs-on: ubuntu-latest
#
#         steps:
#             - name: Docker Build
#               env:
#                 CONTAINER_REGISTRY: docker.io
#                 DOCKER_USERNAME: enter_your_dockerhub_username_here
#                 IMAGE_NAME: github-actions-nginx
#               run:
#                   docker build -t $CONTAINER_REGISTRY/$DOCKER_USERNAME/$IMAGE_NAME:latest
#             - name: Docker Login
#               env:
#                  DOCKER_USERNAME: enter_your_dockerhub_username_here
#                  DOCKER_PASSWORD: enter_your_dockerhub_password_here
#               run:
#                     echo docker login --username=$DOCKER_USERNAME --password=$DOCKER_PASSWORD

#             - name: Docker Publish
#               env:
#                 CONTAINER_REGISTRY: docker.io
#                 DOCKER_USERNAME: enter_your_dockerhub_username_here
#                 IMAGE_NAME: github-actions-nginx
#               run:
#                   docker push $CONTAINER_REGISTRY/$DOCKER_USERNAME/$IMAGE_NAME:latest

#     deploy:
#         needs: [docker]
#         runs-on: ubuntu-latest
#         steps:
#             - name: Deploy Application
#               env:
#                  CONTAINER_REGISTRY: docker.io
#                  DOCKER_USERNAME: enter_your_dockerhub_username_here
#                  IMAGE_NAME: github-actions-nginx
#               run: echo docker run -d -p 8080:80 $CONTAINER_REGISTRY/$DOCKER_USERNAME/$IMAGE_NAME:latest

# Define env at job level
jobs:
    docker:
        runs-on: ubuntu-latest
        steps:
            - name: Docker Build
              run: echo docker build -t ${{ vars.CONTAINER_REGISTRY}}/${{ vars.DOCKER_USERNAME}}/$IMAGE_NAME:latest

            - name: Docker Login
              run: echo docker login --username=${{ vars.DOCKER_USERNAME }} --password=${{ secrets.DOCKER_PASSWORD }}

            - name: Docker Publish
              run: echo docker push $CONTAINER_REGISTRY/${{ vars.DOCKER_USERNAME }}/$IMAGE_NAME:latest

    deploy:
        
        needs: [docker]
        runs-on: ubuntu-latest
        steps:
            - name: Deploy Application
              run: echo docker run -d -p 8080:80 ${{ vars.CONTAINER_REGISTRY}}/${{ vars.DOCKER_USERNAME}}/$IMAGE_NAME:latest
